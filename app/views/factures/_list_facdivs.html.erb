<table>

<% @facture.facdivs.each do |facdiv| %>
  <tr>
    <td><%= facdiv.facture.name %> | </td>
    <td><%= facdiv.date %> | </td>
    <td><%= facdiv.user.name %> | </td>
    <td><%= facdiv.name %> | </td>
    <td><%= facdiv.cout %> € | </td>

    <td><%= link_to image_tag('img-modif.png'), edit_facture_facdiv_url(@facture, facdiv) %> |</td>
    <td><%= link_to image_tag('img-delete.png'), facture_facdiv_url(@facture, facdiv), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />
