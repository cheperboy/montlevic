<h1>New saison</h1>

<% form_for(@saison) do |f| %>
  <%= f.error_messages %>

<table>
	<%= form_tr_text(f, 'Nom', :name) %>
	<%= form_tr_text(f, 'Date', :year) %>
</table>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', saisons_path %>