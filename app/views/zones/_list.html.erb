<table>
  <tr>
    <td>Nom</td>
    <td>surface</td>
  </tr>

<% @zones.each do |zone| %>
  <tr>
    <td><%= zone.name %> | </td>
    <td><%= zone.surface %> Ha | </td>
	
	<% if admin? %>
	    <td><%= link_to 'Modifier', edit_zone_path(zone) %></td>
	    <td><%= link_to 'Supprimer', zone, :confirm => 'Are you sure?', :method => :delete %></td>
	<% end %>
  
  </tr>
<% end %>
</table>
