<% title "Options" %>

<table>
  <tr>
    <th>Saison</th>
  </tr>

<% @settings.each do |setting| %>
  <tr>
    <td><%=h setting.saison.name %></td>
    <td><%=h setting.current %></td>
    <td><%= link_to 'Modifier', edit_setting_path(setting) %></td>
  </tr>
<% end %>
</table>

<br />
